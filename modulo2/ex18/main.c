#include "stdio.h"
#include "asm.h"

int n = 0;

int main(){

	 int result = 0;
	
	 printf("n:");
	 scanf("%d",&n);
	
	 result = somatorio();
	 
	 printf("\n n=%d∑i=1 i^2 ∗ %d^2 / %d = (%d)\n",n,A,B,result);
	 
	 return 0;
}
